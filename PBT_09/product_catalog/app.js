const products=['iPhone 16','MacBook Pro','AirPods Pro','iPad Air','Samsung S24','Dell XPS','Galaxy Buds','Pixel 9','ThinkPad','Xiaomi Pad','Apple Watch','Kindle'].map((name,i)=>({id:i+1,name,price:(i+5)*1990000,category:['phone','laptop','accessory','tablet'][i%4],image:'https://placehold.co/200',rating:4+i%5/10,inStock:i%3!==0}));let state={category:'all',query:'',sort:'name',cart:0};const grid=document.querySelector('#products');function filtered(){let out=products.filter(p=>(state.category==='all'||p.category===state.category)&&p.name.toLowerCase().includes(state.query));out.sort((a,b)=>state.sort==='priceAsc'?a.price-b.price:state.sort==='priceDesc'?b.price-a.price:state.sort==='rating'?b.rating-a.rating:a.name.localeCompare(b.name));return out}function renderProducts(){grid.textContent='';for(const p of filtered()){const card=document.createElement('article');card.className='card';card.innerHTML='<img src="'+p.image+'" alt=""><h2></h2><p></p><button>Them gio</button>';card.querySelector('h2').textContent=p.name;card.querySelector('p').textContent=p.price.toLocaleString('vi-VN')+'d - '+p.category;card.addEventListener('click',()=>showModal(p));card.querySelector('button').addEventListener('click',e=>{e.stopPropagation();state.cart++;document.querySelector('#cart').textContent=state.cart});grid.appendChild(card)}}function showModal(p){document.querySelector('#modal').innerHTML='<section class="modal"><button id="close">Đóng</button><h2>'+p.name+'</h2><p>Rating '+p.rating+'</p></section>';document.querySelector('#close').onclick=()=>document.querySelector('#modal').textContent=''}const cats=['all',...new Set(products.map(p=>p.category))];document.querySelector('#categories').append(...cats.map(c=>{const b=document.createElement('button');b.textContent=c;b.onclick=()=>{state.category=c;renderProducts()};return b}));document.querySelector('#search').addEventListener('input',e=>{state.query=e.target.value.toLowerCase();renderProducts()});document.querySelector('#sort').addEventListener('change',e=>{state.sort=e.target.value;renderProducts()});document.querySelector('#dark').onclick=()=>document.body.classList.toggle('dark-mode');renderProducts();